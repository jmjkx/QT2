LiquidCam/          # 项目根目录（Git仓库根目录）
├── CMakeLists.txt    # 根CMake配置（仅全局配置，关联子模块，不写具体文件）
├── CMakePresets.json # CMake构建预设（统一Qt5路径/编译器/构建目录，多人协作必加）
├── .gitignore        # Git忽略文件（过滤build/编译产物/IDE配置，多人协作必加）
├── main.cpp          # 项目唯一主入口（仅做程序初始化，无业务逻辑）
├── build/            # CMake编译输出目录（Git忽略，多人各自本地生成，互不干扰）
│
├── src/
│   ├── mainui/
│   │   ├── CMakeLists.txt
│   │   ├── ui/
│   │   ├── include/
│   │   ├── src/
│   │   └── biz/
│   └── secondui/
│       ├── CMakeLists.txt
│       ├── ui/
│       ├── include/
│       ├── src/
│       └── biz/
├── resource/              # 全局资源目录（所有模块共用，Git跟踪）
│   ├── images/       # 图片资源（png/ico等，按模块分文件夹）
│   │   ├── secondui
│   │   └── mainui/
│   └── styles/      # 样式资源（qss等，按模块分文件夹）
│       ├── secondui
│       └── mainui/
├── tests/             # 测试代码目录（单元测试/集成测试，多人协作必加）
│
└── 3rd/              # 第三方库目录（如网络库/解析库，多人协作统一版本）
    ├── include/
    └── lib/          # 第三方库库文件

生成一个可以编译运行的qt5 cmake工程模板，确保项目可以编译运行，实现以下功能：
1. 界面设计简洁
2. 代码结构清晰，遵循给定的目录结构和模块划分，确保多人协作开发时易于维护。
3. 尽量多的注释说明代码逻辑和结构
4. 代码风格统一，符合Qt和C++最佳实践，确保代码可读性和可维护性。
也可以修改上述目录结构，但需要保证上述功能。
4. 根目录和子模块都包含 cmakelists.txt, 方便维护，至少包含两个子模块（如mainui和secondui），每个子模块实现一个简单的UI界面。



请根据上述要求生成完整的项目代码。